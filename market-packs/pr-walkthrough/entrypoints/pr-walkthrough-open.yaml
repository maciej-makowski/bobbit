id: pr-walkthrough.open
kind: composer-slash
label: PR Walkthrough
# A composer-slash LAUNCHER (the slash menu). Its selection IS the user gesture;
# it navigates to the deep-link route (no auto-invoke on mount). No hard-coded
# jobId — the panel resolves the real job from the current session.
target:
  route: pr-walkthrough
