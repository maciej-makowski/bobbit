/**
 * Single-project sidebar rendering coverage moved to the deterministic fixture:
 * tests/ui-fixtures/sidebar-filter-search-fixture.spec.ts.
 */
import { test } from "../gateway-harness.js";

test.describe("Single-project sidebar", () => {
	// Intentionally empty: covered by the lightweight fixture.
});
